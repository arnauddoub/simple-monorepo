{
  "name": "my-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm build:packages && vp run -r --parallel dev",
    "build:packages": "vp run -F './packages/*' build"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "packageManager": "pnpm@10.33.3"
}
